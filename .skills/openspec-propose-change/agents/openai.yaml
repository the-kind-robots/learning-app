interface:
  display_name: "OpenSpec Propose"
  short_description: "Create a new OpenSpec change and draft its first artifact."
  default_prompt: "Start a new OpenSpec change for this repo, create the first ready artifact, and summarize what to do next."
policy:
  allow_implicit_invocation: false
