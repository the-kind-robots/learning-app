# Tasks

- [x] 1. Delete the `/goal` skill from `.skills/`
- [x] 2. Remove the empty `.worktrees/` directory and the `.git/worktree-slots*` permission
- [x] 3. Write the branch and worktree rules into `AGENTS.md`
- [x] 4. State single-path branch creation and merge-time cleanup in `repo-task-delivery`
- [x] 5. Drop the worktree-artifact entries from `.gitignore` that only `/goal` produced
- [x] 6. Verify no dangling references to `/goal` outside the OpenSpec archive
