# Tasks

- [x] 1. Extraction script for `LoadCredentialEncrypted=` names + test, wired into infra-checks
- [x] 2. Root-run checker shipped in the deb; sudoers entry in postinst
- [x] 3. Preflight step in deploy-infra.yml: extract names, verify on target, fail with the missing list
