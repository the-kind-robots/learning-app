# Tasks

- [x] 1.1 Words view: `data-vk-overlay` back on `.vocabulary`; the search form becomes the column's last in-flow child, the lesson button stays in the footer.
- [x] 1.2 `vocabulary.css`: the home screen's pattern — footer fixed to the bottom, the column reserving `max(footer height, keyboard-inset-height)` at its foot.
- [x] 1.3 Mobile spec: overlay requested, footer fixed to the viewport bottom, closed-keyboard geometry unchanged. The open keyboard is checked on a phone; headless Chrome has no keyboard inset to give.
- [x] 1.4 Screenshot 390 px, keyboard closed.
