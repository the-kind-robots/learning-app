## 1. Read a word's examples in full

- [x] 1.1 Unit test: 30 examples of one word, one per collection; `of-word` returns all 30
- [x] 1.2 `adapters.examples/of-word` reads through `dbs/find-all`, as `list` and `purge-by-collection!` do
