# Tasks

## 1. Screen

- [x] 1.1 Words view: search form moves into the footer above the lesson button; `data-vk-overlay` dropped so the viewport resizes for the keyboard, as on the lesson.
- [x] 1.2 `vocabulary.css`: the screen is a fixed full-viewport column with the tray in flow; field and button share width; 12px between them; rows start 24px under the bar.

## 2. Verification

- [x] 2.1 Browser spec: field above the button with the same edges, gap under the bar, desktop and 390 px.
- [x] 2.2 Words search/paging specs, mobile project, zprint.
- [x] 2.3 Screenshots: desktop, 390 px, 390 px with a query typed (keyboard emulated by a resized viewport).
