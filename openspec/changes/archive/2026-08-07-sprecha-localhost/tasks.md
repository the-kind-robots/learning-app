# Tasks

- [x] 1. `sprecha.localhost.conf`: port 80, no TLS, `/api/sync/updates` location
- [x] 2. Docs: development-setup section, mobile-pwa-testing origin settings, cross-links, readme
- [x] 3. CDP skill local URL
- [ ] 4. Machine rollout: install the new config, drop `sprecha.local` from /etc/nginx and /etc/hosts, update the tunnel origin, verify from the Windows browser
